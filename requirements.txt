# Python dependencies - run setup_dependencies.sh first to clone these repos
# then install with: pip install -r requirements.txt
-e ./dependencies/bopdftprojections
-e ./dependencies/bopfoxfeaturizer
-e ./dependencies/python-ace
-e ./dependencies/PyCEF

# Optional: BopFox Fortran binary (requires manual compilation from source)
# clone with: git clone git@git.noc.ruhr-uni-bochum.de:fortimtb/bopfox.git dependencies/bopfox

# Local packages
-e ./Tools
