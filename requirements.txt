# Run ./setup_dependencies.sh first to clone all repos and install python-ace.
# Then install remaining dependencies with: pip install -r requirements.txt

-e ./dependencies/bopfoxfeaturizer
-e ./dependencies/PyCEF
numpy<2
seaborn
Jinja2
setuptools<82

# python-ace is installed by setup_dependencies.sh via `python setup.py install`
# (requires patching and C++ compilation, not compatible with pip install)
# optional package
#-e ./dependencies/bopdftprojections

# Local packages
-e ./Tools
