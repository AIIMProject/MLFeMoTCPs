# Run ./setup_dependencies.sh first to clone all repos and install python-ace.
# Then install remaining dependencies with: pip install -r requirements.txt

-e ./dependencies/bopdftprojections
-e ./dependencies/PyCEF

# python-ace installed by setup_dependencies.sh (requires C++ compilation)
# bopfoxfeaturizer installed by setup_dependencies.sh with --no-deps
#   (written against pymatgen 2021.2.16; avoid pulling incompatible version)

# Local packages
-e ./Tools
